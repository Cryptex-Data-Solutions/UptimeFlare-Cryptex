{
  "name": "uptimeflare-aggregator",
  "version": "1.0.0",
  "description": "Aggregator Lambda for UptimeFlare AWS - majority voting, notifications",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js --external:@aws-sdk/*",
    "package": "cd dist && zip -r ../function.zip ."
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "@types/node": "^20.14.0",
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  }
}
